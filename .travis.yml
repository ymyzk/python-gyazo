language: python
python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
  - 3.6
  - nightly
  - pypy
  # - pypy3.3-5.5-alpha
sudo: false
matrix:
  allow_failures:
    - python: nightly
install:
  - pip install -U pip setuptools wheel
  - pip install 'tox>=2.0,<3.0' 'tox-travis>=0.8,<1.0'
script:
  - tox
